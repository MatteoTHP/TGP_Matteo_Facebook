<!doctype html>
<html lang="en">
    <head>
        <title></title>
    </head>
    <body>
        <div class="card text-center">
    
            <h1>Edition de potin!</h1>
        </div>
        
       
            <% if @gossip.errors.any? %>
                <div class="alert alert-danger" role="alert">
                    <p>Nous n'avons pas réussi à éditer le potin pour la (ou les) raison(s) suivante(s) :</p>
                    <ul>
                        <% @gossip.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>  
            <% end %>
      
        <p>entre ces infos pour éditer un gossip: </p>



        <%= form_for @gossip do |f| %>

            <div class="form-group">
                <label> titre de l'article</label>
                
                <%= f.text_field :title, class:'form-control' %>
                <br /> <br />
            </div>

            <div class="form-group">
                <label> Contenu de l'article</label>
                
                <%= f.text_area :content, class:'form-control' %>
                <br /> <br />
            </div>

            <div class="form-group">   
                <button type="submit" class="btn btn-primary">  Edit Gossip </button>
            </div>

        <% end %>

       
    
    </body>
</html>

   

   
    

</html>